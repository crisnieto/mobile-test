version: 2
jobs:
  testapp:
    branches:
      ignore:
        - /.*/
    docker:
      - image: circleci/android:api-27-alpha
    steps:
      - checkout
      - run: pwd
    build:
      branches:
        ignore:
          - /.*/
      docker:
        - image: circleci/android:api-27-alpha
      steps:
        - checkout
        - run: pwd    
      
